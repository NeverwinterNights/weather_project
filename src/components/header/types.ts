export type HeaderPropsType = {
  menuActive: boolean;
  onClickGearHandler: () => void;
};

export type InputsValuesType = {
  cityName: string;
  zipIndex: string;
  zipCode: string;
  coordinatesX: number;
  coordinatesY: number;
  countryID: string;
};
